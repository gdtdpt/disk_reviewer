---
phase: 02-treemap
plan: 05
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - src/ui/info_panel.rs
  - src/ui/mod.rs
  - src/app.rs
autonomous: true
requirements: [VIS-05]

must_haves:
  truths:
    - "选中色块后在详情面板显示完整路径、大小、占比"
    - "详情面板显示文件/子目录数量"
    - "颜色图例显示在详情面板内"
    - "未选中时显示当前目录摘要信息"
  artifacts:
    - path: "src/ui/info_panel.rs"
      provides: "右侧详情面板组件"
      exports: ["info_panel_ui"]
      contains: "pub fn info_panel_ui"
    - path: "src/ui/mod.rs"
      provides: "UI 模块入口更新"
      contains: "pub mod info_panel"
    - path: "src/app.rs"
      provides: "update() 中右侧 SidePanel 布局"
      contains: "SidePanel::right"
  key_links:
    - from: "src/app.rs"
      to: "src/ui/info_panel.rs"
      via: "crate::ui::info_panel::info_panel_ui"
      pattern: "info_panel_ui"
    - from: "src/ui/info_panel.rs"
      to: "src/treemap/types.rs"
      via: "use crate::treemap::TreemapNode"
      pattern: "TreemapNode"
---

<objective>
实现右侧详情面板（VIS-05），包括选中项信息展示和颜色图例（D-10, D-15）。

使用 SidePanel::right 实现 70/30 布局（D-14）。详情面板显示选中节点的路径、大小、占比、文件数，以及所有 10 种文件类型的颜色图例。

Purpose: 用户选中色块后能查看完整信息，颜色图例始终可见帮助理解颜色含义。
Output: 右侧详情面板 + 选中项信息 + 颜色图例
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\Develop\open_code\disk_reviewer\.planning\ROADMAP.md
@D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-CONTEXT.md
@D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-RESEARCH.md
@D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-PATTERNS.md
@D:\Develop\open_code\disk_reviewer\src\scanner\types.rs
@D:\Develop\open_code\disk_reviewer\src\treemap\types.rs
@D:\Develop\open_code\disk_reviewer\src\treemap\color.rs
@D:\Develop\open_code\disk_reviewer\src\app.rs
</context>

<interfaces>
From src/treemap/types.rs:
```rust
pub struct TreemapNode { pub rect: Rect, pub label: String, pub color: Color32, pub depth: u32, pub entry_index: usize, pub is_dir: bool, pub size: u64, pub percentage: f32 }
```

From src/treemap/color.rs:
```rust
pub enum FileCategory { Document, Image, Video, Audio, Archive, Code, Executable, System, Temp, Other }
impl FileCategory {
    pub fn color(&self) -> Color32
    pub fn label(&self) -> &'static str
}
```

From src/scanner/types.rs:
```rust
pub struct DirNode { pub path: PathBuf, pub name: String, pub total_size: u64, pub file_count: u64, pub children: Vec<Entry>, pub access_denied: bool }
```

From src/app.rs (after plan 02-04):
```rust
pub struct DiskReviewerApp {
    pub nav_stack: Vec<usize>,
    pub selected_index: Option<usize>,
    pub treemap_nodes: Vec<TreemapNode>,
    pub scan_result: Option<Arc<DirNode>>,
    ...
}
impl DiskReviewerApp {
    fn current_dir(&self) -> Option<&DirNode> { ... }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 实现详情面板组件 (D-10, D-15)</name>
  <files>src/ui/info_panel.rs</files>
  <read_first>
    - D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-RESEARCH.md (Code Example 3: 详情面板 第 639-682 行)
    - D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-PATTERNS.md (info_panel.rs Analog)
    - D:\Develop\open_code\disk_reviewer\src\treemap\color.rs (FileCategory::label())
  </read_first>
  <action>
    新建 src/ui/info_panel.rs：

    ```rust
    use egui::{Color32, Sense, Ui};
    use crate::treemap::{TreemapNode, FileCategory};
    use crate::treemap::renderer::format_size;
    use crate::scanner::DirNode;

    pub fn info_panel_ui(
        ui: &mut Ui,
        selected: Option<&TreemapNode>,
        current_dir: Option<&DirNode>,
    ) {
        ui.heading("详情");
        ui.separator();

        if let Some(node) = selected {
            ui.label(format!("名称: {}", node.label));
            ui.label(format!("大小: {}", format_size(node.size)));
            ui.label(format!("占比: {:.1}%", node.percentage));
            ui.label(if node.is_dir { "类型: 目录" } else { "类型: 文件" });
            if node.is_dir {
                if let Some(dir) = current_dir {
                    if let Some(crate::scanner::Entry::Dir(d)) = dir.children.get(node.entry_index) {
                        ui.label(format!("文件数: {}", d.file_count));
                        ui.label(format!("子目录: {}", d.children.iter().filter(|c| matches!(c, crate::scanner::Entry::Dir(_))).count()));
                    }
                }
            }
        } else if let Some(dir) = current_dir {
            ui.label(format!("当前: {}", dir.name));
            ui.label(format!("总大小: {}", format_size(dir.total_size)));
            ui.label(format!("文件数: {}", dir.file_count));
            ui.label(format!("子条目: {}", dir.children.len()));
        } else {
            ui.label("点击色块查看详情");
        }

        // D-10, D-15: 颜色图例
        ui.separator();
        ui.heading("图例");
        for cat in [
            FileCategory::Document, FileCategory::Image, FileCategory::Video,
            FileCategory::Audio, FileCategory::Archive, FileCategory::Code,
            FileCategory::Executable, FileCategory::System, FileCategory::Temp,
            FileCategory::Other,
        ] {
            ui.horizontal(|ui| {
                let (rect, _) = ui.allocate_exact_size(
                    egui::vec2(16.0, 16.0),
                    Sense::hover(),
                );
                ui.painter().rect_filled(rect, egui::CornerRadius::same(2.0), cat.color());
                ui.label(cat.label());
            });
        }
    }
    ```

    运行 `cargo check` 编译通过。
    提交：`feat(02-05): implement info panel component`
  </action>
  <verify>
    <automated>cargo check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check` 编译通过
    - `grep -c "pub fn info_panel_ui" src/ui/info_panel.rs` 返回 1
    - 图例包含 10 个 FileCategory 条目
    - 每个图例条目有颜色方块和中文标签
  </acceptance_criteria>
  <done>详情面板组件实现</done>
</task>

<task type="auto">
  <name>Task 2: 集成详情面板到 app.rs 主布局 (D-14)</name>
  <files>src/app.rs, src/ui/mod.rs</files>
  <read_first>
    - D:\Develop\open_code\disk_reviewer\src\app.rs (当前 update() 方法)
    - D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-RESEARCH.md (System Architecture Diagram 第 122-162 行)
    - D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-PATTERNS.md (app.rs UI layout pattern)
  </read_first>
  <action>
    修改 src/app.rs 的 update() 方法，将当前 CentralPanel 布局改为 70/30 左右分割。

    更新 src/ui/mod.rs：
    ```rust
    pub mod breadcrumb;
    pub mod info_panel;

    pub use breadcrumb::breadcrumb_ui;
    pub use info_panel::info_panel_ui;
    ```

    修改 app.rs 的 update() 方法，将 treemap 渲染和详情面板分离：

    ```rust
    fn update(&mut self, ctx: &egui::Context, _frame: &mut eframe::Frame) {
        self.consume_events(ctx);

        // 面包屑在顶部
        if let Some(root) = &self.scan_result {
            egui::TopBottomPanel::top("breadcrumb").show(ctx, |ui| {
                if let Some(depth) = crate::ui::breadcrumb::breadcrumb_ui(
                    ui, root, &self.nav_stack,
                ) {
                    self.navigate_to_depth(depth);
                }
            });
        }

        // 右侧详情面板 (D-14: 固定宽度)
        egui::SidePanel::right("detail_panel")
            .exact_width(320.0)
            .show(ctx, |ui| {
                let selected = self.selected_index
                    .and_then(|i| self.treemap_nodes.get(i));
                let current_dir = self.current_dir();
                crate::ui::info_panel::info_panel_ui(ui, selected, current_dir);
            });

        // 左侧 Treemap 画布 (D-14: 剩余 ~70%)
        egui::CentralPanel::default().show(ctx, |ui| {
            ui.heading("Disk Reviewer");
            ui.separator();

            // 驱动器列表（保留）
            ui.label("逻辑盘:");
            let scan_requests: Vec<std::path::PathBuf> = self.drives.iter().filter_map(|drive| {
                let mut clicked = false;
                ui.horizontal(|ui| {
                    ui.label(format!(
                        "{}: 总计 {:.1} GB  可用 {:.1} GB",
                        drive.letter,
                        drive.total_bytes as f64 / 1e9,
                        drive.free_bytes as f64 / 1e9,
                    ));
                    if ui.button("扫描").clicked() {
                        clicked = true;
                    }
                });
                if clicked {
                    Some(std::path::PathBuf::from(format!(r"{}:\", drive.letter)))
                } else {
                    None
                }
            }).collect();
            for path in scan_requests {
                self.start_scan(path);
            }

            ui.separator();
            ui.label(&self.status_message);

            // Treemap 渲染
            if !self.treemap_nodes.is_empty() {
                ui.separator();
                let canvas_rect = emath::Rect::from_min_size(
                    emath::pos2(0.0, 0.0),
                    emath::vec2(ui.available_width(), ui.available_height().max(200.0)),
                );
                if let Some(dir) = self.current_dir() {
                    self.rebuild_treemap(canvas_rect);
                }
                if let Some(clicked) = crate::treemap::paint_treemap(
                    ui, &self.treemap_nodes, self.selected_index,
                ) {
                    if let Some(dir) = self.current_dir() {
                        if let Some(entry) = dir.children.get(clicked) {
                            if matches!(entry, crate::scanner::Entry::Dir(_)) {
                                self.drill_down(clicked);
                                return;
                            }
                        }
                    }
                    self.selected_index = Some(clicked);
                }
            }
        });
    }
    ```

    需要在 app.rs 顶部添加：
    ```rust
    use emath::{pos2, vec2, Rect};
    use crate::treemap::paint_treemap;
    use crate::ui::breadcrumb::breadcrumb_ui;
    ```

    运行 `cargo check` 编译通过。
    提交：`feat(02-05): integrate info panel and 70/30 layout into app.rs`
  </action>
  <verify>
    <automated>cargo check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check` 编译通过
    - `grep -c "SidePanel::right" src/app.rs` 返回 1
    - `grep -c "exact_width(320.0)" src/app.rs` 返回 1
    - `grep -c "info_panel_ui" src/app.rs` 返回 1
    - `grep -c "TopBottomPanel::top" src/app.rs` 返回 1
    - `grep -c "pub mod info_panel" src/ui/mod.rs` 返回 1
  </acceptance_criteria>
  <done>详情面板集成到主布局</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无 | 纯 UI 展示逻辑，无外部输入或信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | — | — | accept | 纯 UI 展示，无安全威胁 |
</threat_model>

<verification>
- `cargo check` 编译通过
- SidePanel::right 宽度 320px
- 详情面板显示选中项信息和颜色图例
- 面包屑在顶部，Treemap 在左侧，详情面板在右侧
</verification>

<success_criteria>
- 选中色块后详情面板显示名称、大小、占比、类型（VIS-05）
- 选中目录时显示文件数和子目录数
- 未选中时显示当前目录摘要（名称、总大小、文件数、子条目数）
- 颜色图例显示全部 10 种文件类型的颜色方块和中文标签（D-10, D-15）
- 布局为左侧 Treemap ~70%，右侧详情面板 320px（D-14）
- 面包屑在顶部（TopBottomPanel::top）
</success_criteria>

<output>
After completion, create `.planning/phases/02-treemap/02-05-SUMMARY.md`
</output>
