---
phase: 02-treemap
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - src/ui/mod.rs
  - src/ui/breadcrumb.rs
  - src/app.rs
autonomous: true
requirements: [VIS-03, VIS-04]

must_haves:
  truths:
    - "点击目录矩形后进入子目录视图"
    - "面包屑导航显示当前路径"
    - "点击面包屑路径段跳转到对应层级"
    - "下钻后选中状态重置"
  artifacts:
    - path: "src/ui/breadcrumb.rs"
      provides: "面包屑导航组件"
      exports: ["breadcrumb_ui"]
      contains: "pub fn breadcrumb_ui"
    - path: "src/ui/mod.rs"
      provides: "UI 模块入口"
      contains: "pub mod breadcrumb"
    - path: "src/app.rs"
      provides: "drill_down 和 navigate_to_depth 方法 + 面包屑 UI 调用"
      contains: "drill_down"
  key_links:
    - from: "src/app.rs"
      to: "src/ui/breadcrumb.rs"
      via: "crate::ui::breadcrumb::breadcrumb_ui"
      pattern: "breadcrumb_ui"
---

<objective>
实现下钻交互（VIS-03）和面包屑导航（VIS-04）。

在 app.rs 中添加 drill_down() / navigate_to_depth() 方法和 current_dir() 辅助。
创建面包屑 UI 组件，水平排列可点击路径段。
将点击检测从 plan 02-03 的 paint_treemap 连接到 drill_down 逻辑。

Purpose: 用户可以通过点击目录矩形逐层下钻，也可以通过面包屑快速返回任意上层。
Output: 完整的下钻交互 + 面包屑导航
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\Develop\open_code\disk_reviewer\.planning\ROADMAP.md
@D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-CONTEXT.md
@D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-RESEARCH.md
@D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-PATTERNS.md
@D:\Develop\open_code\disk_reviewer\src\scanner\types.rs
@D:\Develop\open_code\disk_reviewer\src\app.rs
@D:\Develop\open_code\disk_reviewer\src\ui\mod.rs
</context>

<interfaces>
From src/app.rs (current state after plan 02-01/02-03):
```rust
pub struct DiskReviewerApp {
    pub drives: Vec<DriveInfo>,
    pub scan_result: Option<Arc<DirNode>>,
    pub scan_progress: Option<ScanEvent>,
    event_receiver: Option<Receiver<ScanEvent>>,
    pub status_message: String,
    cancel_token: Option<Arc<AtomicBool>>,
    pub nav_stack: Vec<usize>,
    pub selected_index: Option<usize>,
    pub treemap_nodes: Vec<crate::treemap::TreemapNode>,
}

impl DiskReviewerApp {
    fn current_dir(&self) -> Option<&DirNode> { ... }
    fn rebuild_treemap(&mut self) { ... }
}
```

From src/scanner/types.rs:
```rust
pub enum Entry { File(FileEntry), Dir(DirNode), Others(OthersEntry), Symlink(PathBuf), AccessDenied { path: PathBuf } }
pub struct DirNode { pub path: PathBuf, pub name: String, pub total_size: u64, pub file_count: u64, pub children: Vec<Entry>, pub access_denied: bool }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 实现面包屑组件 (D-13)</name>
  <files>src/ui/breadcrumb.rs, src/ui/mod.rs</files>
  <read_first>
    - D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-RESEARCH.md (Code Example 2: 面包屑组件 第 609-635 行)
    - D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-PATTERNS.md (breadcrumb.rs Analog)
    - D:\Develop\open_code\disk_reviewer\src\scanner\types.rs (DirNode 结构)
    - D:\Develop\open_code\disk_reviewer\src\ui\mod.rs (现有 stub)
  </read_first>
  <action>
    新建 src/ui/breadcrumb.rs：

    ```rust
    use egui::Ui;
    use crate::scanner::DirNode;

    /// 面包屑导航 UI
    /// 返回用户点击的层级深度（0 = 根），None 表示未点击
    pub fn breadcrumb_ui(
        ui: &mut Ui,
        scan_result: &DirNode,
        nav_stack: &[usize],
    ) -> Option<usize> {
        let mut clicked_depth = None;
        egui::ScrollArea::horizontal().show(ui, |ui| {
            ui.horizontal(|ui| {
                // 根节点按钮
                if ui.button(&scan_result.name).clicked() {
                    clicked_depth = Some(0);
                }
                // 沿 nav_stack 逐级显示路径段
                let mut current = scan_result;
                for (depth, &idx) in nav_stack.iter().enumerate() {
                    ui.label(">");
                    if let Some(crate::scanner::Entry::Dir(dir)) = current.children.get(idx) {
                        if ui.button(&dir.name).clicked() {
                            clicked_depth = Some(depth + 1);
                        }
                        current = dir;
                    }
                }
            });
        });
        clicked_depth
    }
    ```

    更新 src/ui/mod.rs：
    ```rust
    pub mod breadcrumb;
    pub mod info_panel;

    pub use breadcrumb::breadcrumb_ui;
    ```

    运行 `cargo check` 编译通过。
    提交：`feat(02-04): implement breadcrumb navigation component`
  </action>
  <verify>
    <automated>cargo check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check` 编译通过
    - `grep -c "pub fn breadcrumb_ui" src/ui/breadcrumb.rs` 返回 1
    - `grep -c "pub mod breadcrumb" src/ui/mod.rs` 返回 1
    - 面包屑接收 (&DirNode, &[usize]) 参数，返回 Option<usize>
    - 使用 ScrollArea::horizontal 包裹，防止路径过长溢出
  </acceptance_criteria>
  <done>面包屑组件实现</done>
</task>

<task type="auto">
  <name>Task 2: 实现下钻逻辑 + 集成面包屑到 app.rs (D-12, D-13)</name>
  <files>src/app.rs</files>
  <read_first>
    - D:\Develop\open_code\disk_reviewer\src\app.rs (当前完整内容)
    - D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-RESEARCH.md (Pattern 4: 下钻状态管理 第 322-365 行)
    - D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-PATTERNS.md (app.rs MODIFICATION 部分)
  </read_first>
  <action>
    修改 src/app.rs，添加下钻方法并在 update() 中集成面包屑。

    **步骤 A: 在 impl DiskReviewerApp 中添加方法**

    ```rust
    fn drill_down(&mut self, child_index: usize) {
        if let Some(dir) = self.current_dir() {
            if let Some(crate::scanner::Entry::Dir(_)) = dir.children.get(child_index) {
                self.nav_stack.push(child_index);
                self.selected_index = None;
                self.rebuild_treemap();
            }
        }
    }

    fn navigate_to_depth(&mut self, depth: usize) {
        self.nav_stack.truncate(depth);
        self.selected_index = None;
        self.rebuild_treemap();
    }
    ```

    **步骤 B: 修改 rebuild_treemap() 传入实际画布尺寸**

    当前 rebuild_treemap 使用 1x1 占位画布，需要在 update() 中先计算画布区域再布局。
    修改 rebuild_treemap 接受 canvas 参数：
    ```rust
    fn rebuild_treemap(&mut self, canvas: emath::Rect) {
        if let Some(dir) = self.current_dir() {
            self.treemap_nodes = crate::treemap::layout_treemap(dir, canvas);
        } else {
            self.treemap_nodes.clear();
        }
    }
    ```

    **步骤 C: 在 update() 中添加面包屑 + 连接点击**

    在 CentralPanel 顶部（ui.heading 之后，驱动器列表之前）添加面包屑：
    ```rust
    if let Some(root) = &self.scan_result {
        if let Some(depth) = crate::ui::breadcrumb::breadcrumb_ui(
            ui, root, &self.nav_stack,
        ) {
            self.navigate_to_depth(depth);
        }
        ui.separator();
    }
    ```

    **步骤 D: 连接 paint_treemap 点击结果到下钻**

    在 paint_treemap 返回 clicked_index 后，判断是否是目录：
    ```rust
    if let Some(clicked) = crate::treemap::paint_treemap(
        ui, &self.treemap_nodes, self.selected_index,
    ) {
        // 检查是否是目录 -> 下钻
        if let Some(dir) = self.current_dir() {
            if let Some(entry) = dir.children.get(clicked) {
                if matches!(entry, crate::scanner::Entry::Dir(_)) {
                    self.drill_down(clicked);
                    return;
                }
            }
        }
        // 非目录 -> 选中
        self.selected_index = Some(clicked);
    }
    ```

    注意：需要在 app.rs 顶部添加 `use crate::ui::breadcrumb::breadcrumb_ui;`。

    运行 `cargo check` 编译通过。
    提交：`feat(02-04): implement drill-down and integrate breadcrumb into app.rs`
  </action>
  <verify>
    <automated>cargo check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check` 编译通过
    - `grep -c "fn drill_down" src/app.rs` 返回 1
    - `grep -c "fn navigate_to_depth" src/app.rs` 返回 1
    - `grep -c "breadcrumb_ui" src/app.rs` 返回 1
    - drill_down 检查 entry 是 Dir 才 push
    - navigate_to_depth 使用 truncate
    - 下钻后 selected_index = None
  </acceptance_criteria>
  <done>下钻逻辑和面包屑集成完成</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无 | 纯 UI 交互逻辑，输入来自 egui 点击事件，不跨信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | — | — | accept | 导航栈操作受 DirNode children 索引边界保护，越界检查由 get() 隐式完成 |
</threat_model>

<verification>
- `cargo check` 编译通过
- 面包屑组件正确渲染路径段
- drill_down 只对目录条目响应
- navigate_to_depth 正确截断 nav_stack
</verification>

<success_criteria>
- 点击目录矩形进入子目录视图（VIS-03）
- 面包屑显示完整路径，每段可点击（VIS-04）
- 点击面包屑路径段跳转到对应层级
- 下钻后选中状态重置为 None
- nav_stack 正确维护浏览路径
</success_criteria>

<output>
After completion, create `.planning/phases/02-treemap/02-04-SUMMARY.md`
</output>
