---
phase: 02-treemap
plan: 02
type: tdd
wave: 1
depends_on: [02-01]
files_modified:
  - src/treemap/layout.rs
  - src/treemap/mod.rs
  - src/app.rs
autonomous: true
requirements: [VIS-01]

must_haves:
  truths:
    - "布局算法对任意 DirNode 输入都能产生面积正比于 size 的矩形"
    - "所有输出矩形恰好填满画布，无重叠无间隙"
    - "空目录返回空 Vec，不 panic"
    - "单个 child 填充整个画布"
    - "零 size 条目被过滤，不参与布局"
  artifacts:
    - path: "src/treemap/layout.rs"
      provides: "Squarified Treemap 布局算法"
      exports: ["layout_treemap"]
      contains: "pub fn layout_treemap"
    - path: "src/treemap/mod.rs"
      provides: "模块入口更新"
      contains: "pub use layout::layout_treemap"
    - path: "src/app.rs"
      provides: "rebuild_treemap 调用 layout_treemap"
      contains: "layout_treemap"
  key_links:
    - from: "src/treemap/layout.rs"
      to: "src/scanner/types.rs"
      via: "use crate::scanner::{DirNode, Entry}"
      pattern: "use crate::scanner"
    - from: "src/app.rs"
      to: "src/treemap/layout.rs"
      via: "crate::treemap::layout_treemap"
      pattern: "layout_treemap"
---

<objective>
实现 Squarified Treemap 布局算法（D-06），将 DirNode 的 children 转换为面积正比于文件大小的矩形布局。

纯算法模块，100% 单元测试覆盖。输入：&DirNode + 画布 Rect。输出：Vec<TreemapNode>。

Purpose: 这是 VIS-01 需求的核心——将目录树数据转换为可视化矩形布局。
Output: 可独立测试的布局算法，输出面积正比于文件大小的矩形集合
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\Develop\open_code\disk_reviewer\.planning\ROADMAP.md
@D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-CONTEXT.md
@D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-RESEARCH.md
@D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-PATTERNS.md
@D:\Develop\open_code\disk_reviewer\src\scanner\types.rs
@D:\Develop\open_code\disk_reviewer\src\treemap\types.rs
@D:\Develop\open_code\disk_reviewer\src\app.rs
</context>

<interfaces>
From src/scanner/types.rs:
```rust
#[derive(Debug, Clone)]
pub struct DirNode {
    pub path: PathBuf,
    pub name: String,
    pub total_size: u64,
    pub file_count: u64,
    pub children: Vec<Entry>,
    pub access_denied: bool,
}

#[derive(Debug, Clone)]
pub enum Entry {
    File(FileEntry),
    Dir(DirNode),
    Others(OthersEntry),
    Symlink(PathBuf),
    AccessDenied { path: PathBuf },
}

impl Entry {
    pub fn size(&self) -> u64 { ... }
}
```

From src/treemap/types.rs:
```rust
#[derive(Debug, Clone)]
pub struct TreemapNode {
    pub rect: Rect,
    pub label: String,
    pub color: Color32,
    pub depth: u32,
    pub entry_index: usize,
    pub is_dir: bool,
    pub size: u64,
    pub percentage: f32,
}
```
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: RED — 写失败测试</name>
  <files>src/treemap/layout.rs</files>
  <read_first>
    - D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-RESEARCH.md (Pattern 1 算法伪代码 第 190-228 行, Code Example 1 第 449-606 行)
    - D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-PATTERNS.md (layout.rs Analog 和 Test Pattern)
    - D:\Develop\open_code\disk_reviewer\src\scanner\types.rs (DirNode, Entry, FileEntry 定义)
    - D:\Develop\open_code\disk_reviewer\src\treemap\types.rs (TreemapNode 定义)
  </read_first>
  <behavior>
    - 测试 1: 空 children 目录 -> 返回空 Vec
    - 测试 2: 单个 child -> 返回一个 TreemapNode，其 rect 等于整个画布
    - 测试 3: 两个 child (100, 200) -> 两个 TreemapNode，面积比 1:2，总面积等于画布面积
    - 测试 4: 四个 child (100, 200, 300, 400) -> 四个 TreemapNode，面积正比于 size，总面积为 1.0
    - 测试 5: 包含零 size 条目（AccessDenied）-> 零 size 条目被过滤，不产生 TreemapNode
    - 测试 6: 四个 child 相同 size -> 面积均等分配（各 0.25）
  </behavior>
  <action>
    新建 src/treemap/layout.rs，先写测试代码和 `todo!()` 占位实现：

    ```rust
    use crate::scanner::{DirNode, Entry, FileEntry};
    use emath::{pos2, vec2, Rect};
    use std::path::PathBuf;
    use egui::Color32;

    pub fn layout_treemap(dir: &DirNode, canvas: Rect) -> Vec<crate::treemap::TreemapNode> {
        todo!()
    }

    fn entry_name(entry: &Entry) -> String {
        match entry {
            Entry::File(f) => f.name.clone(),
            Entry::Dir(d) => d.name.clone(),
            Entry::Others(o) => o.name.clone(),
            Entry::Symlink(p) => p.file_name()
                .and_then(|n| n.to_str())
                .unwrap_or("?").to_string(),
            Entry::AccessDenied { path } => path.file_name()
                .and_then(|n| n.to_str())
                .unwrap_or("?").to_string(),
        }
    }

    fn is_dir(entry: &Entry) -> bool {
        matches!(entry, Entry::Dir(_))
    }

    #[cfg(test)]
    mod tests {
        use super::*;

        fn make_dir(children: Vec<(String, u64)>) -> DirNode {
            let mut entries = Vec::new();
            for (name, size) in &children {
                entries.push(Entry::File(FileEntry {
                    name: name.clone(),
                    size: *size,
                }));
            }
            let total_size: u64 = entries.iter().map(|e| e.size()).sum();
            DirNode {
                path: PathBuf::from(r"C:\test"),
                name: "test".to_string(),
                total_size,
                file_count: entries.len() as u64,
                children: entries,
                access_denied: false,
            }
        }

        fn canvas() -> Rect {
            Rect::from_min_size(pos2(0.0, 0.0), vec2(1.0, 1.0))
        }

        #[test]
        fn test_empty_dir_returns_empty_vec() {
            let dir = make_dir(vec![]);
            let result = layout_treemap(&dir, canvas());
            assert!(result.is_empty());
        }

        #[test]
        fn test_single_child_fills_canvas() {
            let dir = make_dir(vec![("only.txt".to_string(), 100)]);
            let result = layout_treemap(&dir, canvas());
            assert_eq!(result.len(), 1);
            let n = &result[0];
            assert!((n.rect.min.x - 0.0).abs() < 0.001);
            assert!((n.rect.min.y - 0.0).abs() < 0.001);
            assert!((n.rect.max.x - 1.0).abs() < 0.001);
            assert!((n.rect.max.y - 1.0).abs() < 0.001);
        }

        #[test]
        fn test_two_children_area_ratio() {
            let dir = make_dir(vec![("a.txt".to_string(), 100), ("b.txt".to_string(), 200)]);
            let result = layout_treemap(&dir, canvas());
            assert_eq!(result.len(), 2);
            let area_a = result[0].rect.area();
            let area_b = result[1].rect.area();
            let ratio = area_a / area_b;
            assert!((ratio - 0.5).abs() < 0.05,
                "面积比应接近 0.5，实际 {}", ratio);
            let total = area_a + area_b;
            assert!((total - 1.0).abs() < 0.01);
        }

        #[test]
        fn test_total_area_preserved() {
            let dir = make_dir(vec![
                ("a.txt".to_string(), 100),
                ("b.txt".to_string(), 200),
                ("c.txt".to_string(), 300),
                ("d.txt".to_string(), 400),
            ]);
            let result = layout_treemap(&dir, canvas());
            let total_area: f32 = result.iter().map(|n| n.rect.area()).sum();
            assert!((total_area - 1.0).abs() < 0.01,
                "总面积应等于 1.0，实际 {}", total_area);
        }

        #[test]
        fn test_zero_size_entries_filtered() {
            let mut dir = make_dir(vec![("a.txt".to_string(), 100)]);
            dir.children.push(Entry::AccessDenied { path: PathBuf::from(r"C:\denied") });
            let result = layout_treemap(&dir, canvas());
            assert_eq!(result.len(), 1, "AccessDenied 应被过滤");
        }

        #[test]
        fn test_equal_sizes_equal_areas() {
            let dir = make_dir(vec![
                ("a.txt".to_string(), 100),
                ("b.txt".to_string(), 100),
                ("c.txt".to_string(), 100),
                ("d.txt".to_string(), 100),
            ]);
            let result = layout_treemap(&dir, canvas());
            assert_eq!(result.len(), 4);
            let areas: Vec<f32> = result.iter().map(|n| n.rect.area()).collect();
            for i in 1..areas.len() {
                assert!((areas[i] - areas[0]).abs() < 0.01,
                    "等 size 条目面积应相等");
            }
        }
    }
    ```

    运行 `cargo test treemap::layout::tests -x`，确认所有测试因 todo!() 而 panic/fail。
    提交：`test(02-02): add failing tests for squarified treemap layout`
  </action>
  <verify>
    <automated>cargo test treemap::layout::tests -x 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test treemap::layout::tests -x` 运行后所有 6 个测试均失败
    - `grep -c "#\[test\]" src/treemap/layout.rs` 返回 6
    - `grep -c "todo!" src/treemap/layout.rs` 返回 1
    - git log 最新提交消息包含 `test(02-02): add failing tests for squarified treemap layout`
  </acceptance_criteria>
  <done>6 个失败测试已提交</done>
</task>

<task type="tdd">
  <name>Task 2: GREEN — 实现 Squarified 算法</name>
  <files>src/treemap/layout.rs</files>
  <read_first>
    - D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-RESEARCH.md (Code Example 1 第 449-606 行，包含完整 squarify_recursive, worst_ratio, NRect 实现)
    - D:\Develop\open_code\disk_reviewer\src\treemap\layout.rs (当前含测试和 todo!() 的文件)
  </read_first>
  <behavior>
    - 替换 `todo!()` 为实际实现
    - 所有 6 个测试通过
    - 算法使用 f64 中间计算，最终转换为 f32 Rect
  </behavior>
  <action>
    实现 layout_treemap 函数。基于 RESEARCH.md Code Example 1 的算法：

    替换 `pub fn layout_treemap(dir: &DirNode, canvas: Rect) -> Vec<crate::treemap::TreemapNode> { todo!() }` 为完整实现：

    ```rust
    pub fn layout_treemap(dir: &DirNode, canvas: Rect) -> Vec<crate::treemap::TreemapNode> {
        let total_size = dir.total_size as f64;
        if total_size == 0.0 {
            return Vec::new();
        }

        // 1. 收集有效条目（size > 0），记录原始索引
        let mut items: Vec<(usize, u64, String, bool)> = Vec::new();
        for (idx, child) in dir.children.iter().enumerate() {
            let size = child.size();
            if size == 0 { continue; }
            items.push((idx, size, entry_name(child), is_dir(child)));
        }

        if items.is_empty() {
            return Vec::new();
        }

        // 2. 按 size 降序
        items.sort_by_key(|&(_, size, _, _)| std::cmp::Reverse(size));

        // 3. 归一化布局
        let sizes: Vec<f64> = items.iter().map(|&(_, s, _, _)| s as f64).collect();
        let nrects = squarify_recursive(&sizes, 0.0, 0.0, 1.0, 1.0);

        // 4. 缩放到画布 + 组装 TreemapNode
        items.into_iter().zip(nrects.into_iter())
            .map(|((entry_index, size, label, is_dir), nr)| {
                let rect = Rect::from_min_size(
                    pos2(
                        canvas.min.x + nr.x * canvas.width(),
                        canvas.min.y + nr.y * canvas.height(),
                    ),
                    vec2(
                        nr.w * canvas.width(),
                        nr.h * canvas.height(),
                    ),
                );
                crate::treemap::TreemapNode {
                    rect,
                    label,
                    color: Color32::from_rgb(150, 150, 150), // 临时灰色，plan 02-03 替换为颜色映射
                    depth: 0,
                    entry_index,
                    is_dir,
                    size,
                    percentage: (size as f64 / total_size * 100.0) as f32,
                }
            })
            .collect()
    }

    #[derive(Clone, Copy)]
    struct NRect { x: f32, y: f32, w: f32, h: f32 }

    fn squarify_recursive(sizes: &[f64], x: f32, y: f32, w: f32, h: f32) -> Vec<NRect> {
        let n = sizes.len();
        if n == 0 { return Vec::new(); }
        if n == 1 { return vec![NRect { x, y, w, h }]; }

        let total: f64 = sizes.iter().sum();
        if total == 0.0 { return Vec::new(); }

        let short_side = w.min(h);
        let long_side = w.max(h);
        let mut row = vec![sizes[0]];
        let mut row_sum = sizes[0];
        let mut remaining = &sizes[1..];

        while !remaining.is_empty() {
            let current_worst = worst_ratio(&row, row_sum, short_side, long_side, total);
            let mut new_row = row.clone();
            new_row.push(remaining[0]);
            let new_worst = worst_ratio(&new_row, row_sum + remaining[0], short_side, long_side, total);
            if new_worst <= current_worst {
                row_sum += remaining[0];
                row = new_row;
                remaining = &remaining[1..];
            } else {
                break;
            }
        }

        let row_total: f64 = row.iter().sum();
        let row_ratio = (row_total / total) as f32;
        let mut result = Vec::new();
        let mut offset = 0.0f32;

        if w >= h {
            let row_w = row_ratio * w;
            for &size in &row {
                let sw = (size as f32 / row_total as f32) * row_w;
                result.push(NRect { x: x + offset, y, w: sw, h });
                offset += sw;
            }
            result.extend(squarify_recursive(remaining, x + row_w, y, w - row_w, h));
        } else {
            let row_h = row_ratio * h;
            for &size in &row {
                let sh = (size as f32 / row_total as f32) * row_h;
                result.push(NRect { x, y: y + offset, w, h: sh });
                offset += sh;
            }
            result.extend(squarify_recursive(remaining, x, y + row_h, w, h - row_h));
        }
        result
    }

    fn worst_ratio(row: &[f64], row_sum: f64, short_side: f32, long_side: f32, total: f64) -> f32 {
        if row.is_empty() || row_sum == 0.0 || total == 0.0 {
            return f32::MAX;
        }
        let row_long = (row_sum as f32 / total as f32) * long_side;
        let row_short = short_side;
        row.iter().map(|&s| {
            let s = s as f32;
            let w = (s / row_sum as f32) * row_long;
            let h = (s / row_sum as f32) * row_short;
            let mn = w.min(h);
            let mx = w.max(h);
            if mn <= 0.0 { f32::MAX } else { mx / mn }
        }).fold(0.0f32, f32::max)
    }
    ```

    运行 `cargo test treemap::layout::tests -x`，确认所有 6 个测试通过。
    提交：`feat(02-02): implement squarified treemap layout algorithm`
  </action>
  <verify>
    <automated>cargo test treemap::layout::tests -x 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test treemap::layout::tests -x` 所有 6 个测试通过
    - `grep -c "todo!" src/treemap/layout.rs` 返回 0
    - `grep -c "fn squarify_recursive" src/treemap/layout.rs` 返回 1
    - `grep -c "fn worst_ratio" src/treemap/layout.rs` 返回 1
    - `grep -c "struct NRect" src/treemap/layout.rs` 返回 1
    - git log 最新提交消息包含 `feat(02-02): implement squarified treemap layout algorithm`
  </acceptance_criteria>
  <done>布局算法实现，所有测试通过</done>
</task>

<task type="auto">
  <name>Task 3: 连接 layout_treemap 到模块和 app.rs</name>
  <files>src/treemap/mod.rs, src/app.rs</files>
  <read_first>
    - D:\Develop\open_code\disk_reviewer\src\treemap\mod.rs (当前内容)
    - D:\Develop\open_code\disk_reviewer\src\app.rs (rebuild_treemap 占位实现)
  </read_first>
  <action>
    1. 更新 src/treemap/mod.rs，添加 layout 模块声明和 re-export：
    ```rust
    pub mod types;
    pub mod layout;

    pub use types::TreemapNode;
    pub use layout::layout_treemap;
    ```

    2. 更新 src/app.rs 中的 rebuild_treemap() 方法，取消注释并调用 layout_treemap：
    ```rust
    fn rebuild_treemap(&mut self) {
        if let Some(dir) = self.current_dir() {
            // 画布 Rect 在 update() 中计算，此处先用零尺寸占位
            // plan 02-03 中会传入实际画布尺寸
            self.treemap_nodes = crate::treemap::layout_treemap(
                dir,
                emath::Rect::from_min_size(emath::pos2(0.0, 0.0), emath::vec2(1.0, 1.0)),
            );
        }
    }
    ```

    需要在 app.rs 顶部添加 emath import（如果尚未存在）：
    ```rust
    use emath::{pos2, vec2, Rect};
    ```

    运行 `cargo check` 确认编译通过。
    提交：`feat(02-02): wire layout_treemap into module exports and app.rs`
  </action>
  <verify>
    <automated>cargo check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check` 编译通过
    - `grep -c "pub mod layout" src/treemap/mod.rs` 返回 1
    - `grep -c "pub use layout::layout_treemap" src/treemap/mod.rs` 返回 1
    - `grep -c "layout_treemap" src/app.rs` 返回 1
  </acceptance_criteria>
  <done>layout_treemap 已连接到模块系统和 app.rs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无 | 纯算法模块，无外部输入或信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | DoS | layout_treemap | accept | 输入来自 Phase 1 扫描结果，条目数受 Others 聚合限制（<=550），不会导致栈溢出 |
</threat_model>

<verification>
- `cargo test treemap::layout::tests -x` 全部通过
- `cargo check` 编译通过
- 布局算法对空/单/多/含零 size 条目场景均有测试覆盖
</verification>

<success_criteria>
- 6 个单元测试全部通过，覆盖空目录、单条目、多条目、零 size 过滤、等面积场景
- layout_treemap 通过 treemap::mod.rs 对外暴露
- app.rs 的 rebuild_treemap() 调用 layout_treemap
- 提交序列：test(...) -> feat(...) -> feat(...) (RED -> GREEN -> wire)
</success_criteria>

<output>
After completion, create `.planning/phases/02-treemap/02-02-SUMMARY.md`
</output>
