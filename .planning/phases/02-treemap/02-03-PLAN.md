---
phase: 02-treemap
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - src/treemap/color.rs
  - src/treemap/renderer.rs
  - src/treemap/mod.rs
  - src/treemap/layout.rs
  - src/app.rs
autonomous: true
requirements: [VIS-02]

must_haves:
  truths:
    - "文件扩展名正确映射到 10 种文件类型之一"
    - "目录颜色由其主导文件类型决定"
    - "Treemap 矩形通过 egui Painter 正确渲染"
    - "色块显示名称标签（面积 >= 400 平方像素时）"
    - "小矩形不显示标签，悬停时 tooltip 显示名称和大小"
  artifacts:
    - path: "src/treemap/color.rs"
      provides: "FileCategory 枚举 + 颜色映射 + 分类函数"
      exports: ["FileCategory"]
      contains: "pub enum FileCategory"
    - path: "src/treemap/renderer.rs"
      provides: "egui Painter 渲染入口"
      contains: "pub fn paint_treemap"
    - path: "src/treemap/mod.rs"
      provides: "模块入口更新"
      contains: "pub mod color"
    - path: "src/treemap/layout.rs"
      provides: "layout_treemap 使用 FileCategory 着色"
      contains: "FileCategory"
    - path: "src/app.rs"
      provides: "update() 中分配渲染区域并调用 paint_treemap"
      contains: "paint_treemap"
  key_links:
    - from: "src/treemap/color.rs"
      to: "src/scanner/types.rs"
      via: "use crate::scanner::Entry"
      pattern: "use crate::scanner"
    - from: "src/treemap/layout.rs"
      to: "src/treemap/color.rs"
      via: "categorize_entry"
      pattern: "categorize_entry"
    - from: "src/app.rs"
      to: "src/treemap/renderer.rs"
      via: "paint_treemap"
      pattern: "paint_treemap"
---

<objective>
实现文件类型颜色映射（D-09）和 egui Treemap 渲染（VIS-02）。

颜色模块使用 TDD（分类逻辑可单元测试），渲染模块为 execute 类型（egui Painter 无法单元测试）。
将颜色映射集成到 layout_treemap 中，使输出 TreemapNode 携带正确颜色。
在 app.rs 的 update() 中添加渲染调用。

Purpose: 让扫描结果以彩色矩形树图的形式呈现给用户，每个色块大小正比于空间占用，颜色反映文件类型。
Output: 彩色 Treemap 渲染 + 标签显示 + 悬停 tooltip
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\Develop\open_code\disk_reviewer\.planning\ROADMAP.md
@D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-CONTEXT.md
@D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-RESEARCH.md
@D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-PATTERNS.md
@D:\Develop\open_code\disk_reviewer\src\scanner\types.rs
@D:\Develop\open_code\disk_reviewer\src\treemap\types.rs
@D:\Develop\open_code\disk_reviewer\src\treemap\layout.rs
@D:\Develop\open_code\disk_reviewer\src\app.rs
</context>

<interfaces>
From src/scanner/types.rs:
```rust
pub enum Entry {
    File(FileEntry), Dir(DirNode), Others(OthersEntry), Symlink(PathBuf), AccessDenied { path: PathBuf }
}
pub struct FileEntry { pub name: String, pub size: u64 }
pub struct DirNode { pub path: PathBuf, pub name: String, pub total_size: u64, pub file_count: u64, pub children: Vec<Entry>, pub access_denied: bool }
impl Entry { pub fn size(&self) -> u64 }
```

From src/treemap/types.rs:
```rust
pub struct TreemapNode { pub rect: Rect, pub label: String, pub color: Color32, pub depth: u32, pub entry_index: usize, pub is_dir: bool, pub size: u64, pub percentage: f32 }
```

From src/treemap/layout.rs:
```rust
pub fn layout_treemap(dir: &DirNode, canvas: Rect) -> Vec<TreemapNode>
```
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: 文件类型颜色映射 RED + GREEN (D-09)</name>
  <files>src/treemap/color.rs</files>
  <read_first>
    - D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-RESEARCH.md (Pattern 5: 颜色映射 第 369-403 行)
    - D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-PATTERNS.md (Data Model Pattern, Test Pattern)
    - D:\Develop\open_code\disk_reviewer\src\scanner\types.rs (Entry 定义)
  </read_first>
  <behavior>
    - 测试: .txt -> Document, .jpg -> Image, .mp4 -> Video, .mp3 -> Audio, .zip -> Archive
    - 测试: .rs -> Code, .exe -> Executable, .dll -> System, .tmp -> Temp, .xyz -> Other
    - 测试: Document 颜色为 RGB(70, 130, 180)
    - 测试: dominant_category 对纯文档目录返回 Document
  </behavior>
  <action>
    新建 src/treemap/color.rs。先写测试再实现（RED -> GREEN 可在同一任务完成）。

    完整文件内容：

    ```rust
    use crate::scanner::{DirNode, Entry, FileEntry};
    use egui::Color32;
    use std::path::Path;

    #[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
    pub enum FileCategory {
        Document, Image, Video, Audio, Archive,
        Code, Executable, System, Temp, Other,
    }

    impl FileCategory {
        pub fn color(&self) -> Color32 {
            match self {
                FileCategory::Document   => Color32::from_rgb(70, 130, 180),
                FileCategory::Image      => Color32::from_rgb(46, 139, 87),
                FileCategory::Video      => Color32::from_rgb(220, 20, 60),
                FileCategory::Audio      => Color32::from_rgb(255, 140, 0),
                FileCategory::Archive    => Color32::from_rgb(128, 0, 128),
                FileCategory::Code       => Color32::from_rgb(0, 128, 128),
                FileCategory::Executable => Color32::from_rgb(184, 134, 11),
                FileCategory::System     => Color32::from_rgb(192, 192, 192),
                FileCategory::Temp       => Color32::from_rgb(169, 169, 169),
                FileCategory::Other      => Color32::from_rgb(105, 105, 105),
            }
        }

        pub fn label(&self) -> &'static str {
            match self {
                FileCategory::Document   => "文档",
                FileCategory::Image      => "图片",
                FileCategory::Video      => "视频",
                FileCategory::Audio      => "音频",
                FileCategory::Archive    => "压缩包",
                FileCategory::Code       => "代码",
                FileCategory::Executable => "可执行",
                FileCategory::System     => "系统",
                FileCategory::Temp       => "临时",
                FileCategory::Other      => "其他",
            }
        }
    }

    pub fn categorize(path: &Path) -> FileCategory {
        let ext = path.extension()
            .and_then(|e| e.to_str())
            .unwrap_or("")
            .to_lowercase();
        match ext.as_str() {
            "txt" | "doc" | "docx" | "pdf" | "xls" | "xlsx" | "ppt" | "pptx" | "rtf" | "odt" => FileCategory::Document,
            "jpg" | "jpeg" | "png" | "gif" | "bmp" | "svg" | "webp" | "ico" | "tiff" | "tif" | "raw" | "heic" => FileCategory::Image,
            "mp4" | "avi" | "mkv" | "mov" | "wmv" | "flv" | "webm" | "m4v" | "mpg" | "mpeg" | "ts" => FileCategory::Video,
            "mp3" | "wav" | "flac" | "aac" | "ogg" | "wma" | "m4a" | "opus" | "ape" => FileCategory::Audio,
            "zip" | "rar" | "7z" | "tar" | "gz" | "bz2" | "xz" | "lz" | "cab" | "iso" => FileCategory::Archive,
            "rs" | "py" | "js" | "ts" | "java" | "c" | "cpp" | "h" | "hpp" | "go" | "rb" | "php" | "swift" | "kt" | "scala" | "html" | "css" | "xml" | "json" | "yaml" | "yml" | "toml" | "sql" | "sh" | "bat" | "ps1" => FileCategory::Code,
            "exe" | "msi" | "com" | "scr" => FileCategory::Executable,
            "dll" | "sys" | "drv" | "ocx" | "cpl" | "efi" => FileCategory::System,
            "tmp" | "temp" | "bak" | "old" | "log" | "cache" => FileCategory::Temp,
            _ => FileCategory::Other,
        }
    }

    pub fn categorize_entry(entry: &Entry) -> FileCategory {
        match entry {
            Entry::File(f) => categorize(Path::new(&f.name)),
            Entry::Dir(d) => categorize(Path::new(&d.name)),
            Entry::Others(o) => categorize(Path::new(&o.name)),
            Entry::Symlink(p) => categorize(p),
            Entry::AccessDenied { path } => categorize(path),
        }
    }

    pub fn dominant_category(dir: &DirNode) -> FileCategory {
        let mut size_by_cat: std::collections::HashMap<FileCategory, u64> = std::collections::HashMap::new();
        accumulate_categories(dir, &mut size_by_cat);
        size_by_cat.into_iter()
            .max_by_key(|&(_, size)| size)
            .map(|(cat, _)| cat)
            .unwrap_or(FileCategory::Other)
    }

    fn accumulate_categories(dir: &DirNode, acc: &mut std::collections::HashMap<FileCategory, u64>) {
        for child in &dir.children {
            match child {
                Entry::Dir(d) => accumulate_categories(d, acc),
                _ => {
                    let cat = categorize_entry(child);
                    *acc.entry(cat).or_insert(0) += child.size();
                }
            }
        }
    }

    #[cfg(test)]
    mod tests {
        use super::*;

        #[test]
        fn test_categorize_document() {
            assert_eq!(categorize(Path::new("test.txt")), FileCategory::Document);
            assert_eq!(categorize(Path::new("report.pdf")), FileCategory::Document);
            assert_eq!(categorize(Path::new("data.xlsx")), FileCategory::Document);
        }

        #[test]
        fn test_categorize_image() {
            assert_eq!(categorize(Path::new("photo.jpg")), FileCategory::Image);
            assert_eq!(categorize(Path::new("icon.png")), FileCategory::Image);
        }

        #[test]
        fn test_categorize_video() {
            assert_eq!(categorize(Path::new("movie.mp4")), FileCategory::Video);
            assert_eq!(categorize(Path::new("clip.mkv")), FileCategory::Video);
        }

        #[test]
        fn test_categorize_audio() {
            assert_eq!(categorize(Path::new("song.mp3")), FileCategory::Audio);
            assert_eq!(categorize(Path::new("track.flac")), FileCategory::Audio);
        }

        #[test]
        fn test_categorize_archive() {
            assert_eq!(categorize(Path::new("data.zip")), FileCategory::Archive);
            assert_eq!(categorize(Path::new("backup.7z")), FileCategory::Archive);
        }

        #[test]
        fn test_categorize_code() {
            assert_eq!(categorize(Path::new("main.rs")), FileCategory::Code);
            assert_eq!(categorize(Path::new("app.py")), FileCategory::Code);
        }

        #[test]
        fn test_categorize_executable() {
            assert_eq!(categorize(Path::new("app.exe")), FileCategory::Executable);
            assert_eq!(categorize(Path::new("setup.msi")), FileCategory::Executable);
        }

        #[test]
        fn test_categorize_system() {
            assert_eq!(categorize(Path::new("kernel.dll")), FileCategory::System);
            assert_eq!(categorize(Path::new("driver.sys")), FileCategory::System);
        }

        #[test]
        fn test_categorize_temp() {
            assert_eq!(categorize(Path::new("cache.tmp")), FileCategory::Temp);
            assert_eq!(categorize(Path::new("old.log")), FileCategory::Temp);
        }

        #[test]
        fn test_categorize_other() {
            assert_eq!(categorize(Path::new("data.xyz")), FileCategory::Other);
            assert_eq!(categorize(Path::new("noext")), FileCategory::Other);
        }

        #[test]
        fn test_document_color() {
            let c = FileCategory::Document.color();
            assert_eq!(c.r(), 70);
            assert_eq!(c.g(), 130);
            assert_eq!(c.b(), 180);
        }

        #[test]
        fn test_dominant_category_documents() {
            let dir = DirNode {
                path: PathBuf::from(r"C:\docs"),
                name: "docs".to_string(),
                total_size: 300,
                file_count: 3,
                children: vec![
                    Entry::File(FileEntry { name: "a.txt".to_string(), size: 100 }),
                    Entry::File(FileEntry { name: "b.pdf".to_string(), size: 100 }),
                    Entry::File(FileEntry { name: "c.jpg".to_string(), size: 100 }),
                ],
                access_denied: false,
            };
            let dom = dominant_category(&dir);
            assert_eq!(dom, FileCategory::Document);
        }
    }
    ```

    先只写测试 + `fn categorize(_: &Path) -> FileCategory { todo!() }` 等占位，运行 `cargo test treemap::color::tests -x` 确认失败。
    提交 RED: `test(02-03): add failing tests for file category color mapping`

    然后实现完整代码，运行 `cargo test treemap::color::tests -x` 确认全部通过。
    提交 GREEN: `feat(02-03): implement file category color mapping`
  </action>
  <verify>
    <automated>cargo test treemap::color::tests -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test treemap::color::tests -x` 所有 12 个测试通过
    - `grep -c "pub enum FileCategory" src/treemap/color.rs` 返回 1
    - `grep -c "pub fn categorize" src/treemap/color.rs` 返回 1
    - `grep -c "pub fn dominant_category" src/treemap/color.rs` 返回 1
    - `grep -c "pub fn label" src/treemap/color.rs` 返回 1
    - FileCategory 有 10 个变体
    - git log 包含 test(...) 和 feat(...) 提交
  </acceptance_criteria>
  <done>颜色映射实现，所有测试通过</done>
</task>

<task type="auto">
  <name>Task 2: 更新 layout_treemap 使用颜色映射</name>
  <files>src/treemap/layout.rs</files>
  <read_first>
    - D:\Develop\open_code\disk_reviewer\src\treemap\layout.rs (当前 layout_treemap 实现)
    - D:\Develop\open_code\disk_reviewer\src\treemap\color.rs (刚实现的 FileCategory)
  </read_first>
  <action>
    修改 src/treemap/layout.rs，将临时灰色替换为基于 FileCategory 的颜色。

    1. 添加 import：
    ```rust
    use crate::treemap::color::{categorize_entry, dominant_category};
    ```

    2. 重构 items 收集逻辑，保留 &Entry 引用：
    将 items 类型改为 `Vec<(usize, &Entry, u64)>`。

    3. 在组装 TreemapNode 时计算颜色：
    ```rust
    .map(|((entry_index, entry, size), nr)| {
        let cat = if let Entry::Dir(d) = entry {
            dominant_category(d)
        } else {
            categorize_entry(entry)
        };
        crate::treemap::TreemapNode {
            rect,
            label: entry_name(entry),
            color: cat.color(),
            depth: 0,
            entry_index,
            is_dir: matches!(entry, Entry::Dir(_)),
            size,
            percentage: (size as f64 / total_size * 100.0) as f32,
        }
    })
    ```

    运行 `cargo test treemap::layout::tests -x` 确认布局测试仍然通过。
    提交：`feat(02-03): integrate color mapping into layout_treemap`
  </action>
  <verify>
    <automated>cargo test treemap::layout::tests -x 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test treemap::layout::tests -x` 所有测试通过
    - `grep -c "categorize_entry" src/treemap/layout.rs` >= 1
    - `grep -c "Color32::from_rgb(150, 150, 150)" src/treemap/layout.rs` 返回 0
  </acceptance_criteria>
  <done>layout_treemap 输出带正确颜色的 TreemapNode</done>
</task>

<task type="auto">
  <name>Task 3: 实现 egui Treemap 渲染器 + 集成到 app.rs</name>
  <files>src/treemap/renderer.rs, src/treemap/mod.rs, src/app.rs</files>
  <read_first>
    - D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-RESEARCH.md (Pattern 3: egui 自定义渲染 + 交互 第 256-318 行)
    - D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-PATTERNS.md (renderer.rs Analog)
    - D:\Develop\open_code\disk_reviewer\src\app.rs (update() 方法)
  </read_first>
  <action>
    本任务分三步：创建渲染器、更新模块导出、集成到 app.rs。

    步骤 A: 新建 src/treemap/renderer.rs：

    ```rust
    use egui::{Color32, CornerRadius, FontId, Sense, Stroke, StrokeKind, Ui, emath};
    use crate::treemap::TreemapNode;

    const LABEL_AREA_THRESHOLD: f32 = 400.0;

    pub fn paint_treemap(
        ui: &mut Ui,
        nodes: &[TreemapNode],
        selected_index: Option<usize>,
    ) -> Option<usize> {
        let size = ui.available_size();
        let (response, painter) = ui.allocate_painter(size, Sense::click());
        let mut clicked_index = None;

        for (i, node) in nodes.iter().enumerate() {
            if !response.rect.intersects(node.rect) { continue; }
            painter.rect_filled(node.rect, CornerRadius::same(1.0), node.color);
            if selected_index == Some(i) {
                painter.rect_stroke(
                    node.rect.shrink(1.0),
                    CornerRadius::same(1.0),
                    Stroke::new(2.0, Color32::WHITE),
                    StrokeKind::Middle,
                );
            }
            let area = node.rect.width() * node.rect.height();
            if area >= LABEL_AREA_THRESHOLD {
                painter.text(
                    node.rect.left_top() + emath::vec2(2.0, 2.0),
                    egui::Align2::LEFT_TOP,
                    &node.label,
                    FontId::proportional(12.0),
                    Color32::WHITE,
                );
            }
        }

        if response.clicked() {
            if let Some(pos) = response.interact_pointer_pos() {
                for (i, node) in nodes.iter().enumerate().rev() {
                    if node.rect.contains(pos) {
                        clicked_index = Some(i);
                        break;
                    }
                }
            }
        }

        if let Some(pos) = response.hover_pos() {
            for node in nodes.iter().rev() {
                if node.rect.contains(pos) {
                    let size_str = format_size(node.size);
                    response.on_hover_ui_at_pointer(|ui| {
                        ui.label(&node.label);
                        ui.label(size_str);
                        ui.label(format!("{:.1}%", node.percentage));
                    });
                    break;
                }
            }
        }

        clicked_index
    }

    pub fn format_size(bytes: u64) -> String {
        const UNITS: &[&str] = &["B", "KB", "MB", "GB", "TB"];
        let mut size = bytes as f64;
        let mut i = 0;
        while size >= 1024.0 && i < UNITS.len() - 1 {
            size /= 1024.0;
            i += 1;
        }
        format!("{:.1} {}", size, UNITS[i])
    }
    ```

    步骤 B: 更新 src/treemap/mod.rs 添加 renderer 模块声明和 re-export。

    步骤 C: 更新 src/app.rs 的 update() 方法，在 status_message 后添加 treemap 渲染区域：
    ```rust
    if !self.treemap_nodes.is_empty() {
        ui.separator();
        ui.label("空间分布:");
        let canvas_rect = Rect::from_min_size(
            pos2(0.0, 0.0),
            vec2(ui.available_width(), ui.available_height().max(200.0)),
        );
        if let Some(dir) = self.current_dir() {
            self.treemap_nodes = crate::treemap::layout_treemap(dir, canvas_rect);
        }
        if let Some(clicked) = crate::treemap::paint_treemap(
            ui, &self.treemap_nodes, self.selected_index,
        ) {
            self.selected_index = Some(clicked);
        }
    }
    ```

    在 app.rs 顶部添加 `use emath::{pos2, vec2, Rect};` 和 `use crate::treemap::paint_treemap;`。

    运行 `cargo check` 确认编译通过。
    提交：`feat(02-03): implement egui treemap renderer and integrate into app.rs`
  </action>
  <verify>
    <automated>cargo check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check` 编译通过
    - `grep -c "pub fn paint_treemap" src/treemap/renderer.rs` 返回 1
    - `grep -c "pub fn format_size" src/treemap/renderer.rs` 返回 1
    - `grep -c "pub mod renderer" src/treemap/mod.rs` 返回 1
    - `grep -c "paint_treemap" src/app.rs` 返回 1
    - `grep -c "LABEL_AREA_THRESHOLD" src/treemap/renderer.rs` 返回 1
  </acceptance_criteria>
  <done>渲染器实现并集成到 app.rs，编译通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无 | 本 plan 仅涉及渲染和颜色映射，无外部输入或信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | — | — | accept | 纯渲染逻辑，无安全威胁 |
</threat_model>

<verification>
- `cargo test treemap::color::tests -x` 全部通过
- `cargo test treemap::layout::tests -x` 全部通过
- `cargo check` 编译通过
- TreemapNode 携带基于 FileCategory 的颜色
- paint_treemap 在 app.rs 的 update() 中被调用
</verification>

<success_criteria>
- FileCategory 枚举有 10 个变体，每个有对应的颜色和中文标签
- categorize() 正确映射文件扩展名到对应类别
- dominant_category() 递归统计目录主导类型
- 12 个颜色映射单元测试全部通过
- paint_treemap 使用 egui Painter 绘制矩形、标签、选中高亮
- 面积 < 400 平方像素的矩形不显示标签
- 悬停时 tooltip 显示名称、大小、占比
- app.rs update() 中调用 paint_treemap
</success_criteria>

<output>
After completion, create `.planning/phases/02-treemap/02-03-SUMMARY.md`
</output>
