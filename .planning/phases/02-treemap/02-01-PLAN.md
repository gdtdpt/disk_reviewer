---
phase: 02-treemap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/treemap/mod.rs
  - src/treemap/types.rs
  - src/app.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "treemap 模块可以被编译，类型可被外部引用"
    - "app.rs 增加了 treemap 相关字段，新字段初始化为空/默认值"
    - "扫描完成后 current_node 自动设置为扫描根节点"
    - "应用在不扫描时仍正常启动和显示"
  artifacts:
    - path: "src/treemap/mod.rs"
      provides: "模块入口，re-export TreemapNode, layout_treemap, FileCategory"
      exports: ["TreemapNode", "layout_treemap", "FileCategory"]
    - path: "src/treemap/types.rs"
      provides: "TreemapNode struct 定义"
      contains: "pub struct TreemapNode"
    - path: "src/app.rs"
      provides: "扩展的 DiskReviewerApp 结构体"
      contains: "nav_stack"
  key_links:
    - from: "src/treemap/mod.rs"
      to: "src/treemap/types.rs"
      via: "pub use types::TreemapNode"
      pattern: "pub use types::TreemapNode"
    - from: "src/app.rs"
      to: "src/treemap/mod.rs"
      via: "use crate::treemap"
      pattern: "use crate::treemap"
---

<objective>
创建 Treemap 模块的工程脚手架和数据结构定义，扩展 app.rs 以支持 treemap 状态管理。

Phase 2 所有后续 plan 的编译和类型依赖基础。不涉及任何算法实现，纯类型定义 + 模块结构 + 状态字段添加。

Purpose: 建立模块边界、类型定义、状态字段，使 plan 02-02 (布局算法) 有可消费的输入类型和输出目标。
Output: 可编译的 treemap 模块、TreemapNode 结构体、app.rs 导航状态字段
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\Develop\open_code\disk_reviewer\.planning\ROADMAP.md
@D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-CONTEXT.md
@D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-RESEARCH.md
@D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-PATTERNS.md
@D:\Develop\open_code\disk_reviewer\src\scanner\types.rs
@D:\Develop\open_code\disk_reviewer\src\scanner\mod.rs
@D:\Develop\open_code\disk_reviewer\src\app.rs
@D:\Develop\open_code\disk_reviewer\src\main.rs
@D:\Develop\open_code\disk_reviewer\Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 TreemapNode 结构体 (D-07)</name>
  <files>src/treemap/types.rs</files>
  <read_first>
    - D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-RESEARCH.md (Pattern 2: TreemapNode struct 定义, 第 238-252 行)
    - D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-PATTERNS.md (Data Model Pattern)
    - D:\Develop\open_code\disk_reviewer\src\scanner\types.rs (DirNode 结构体作为参考模式)
    - D:\Develop\open_code\disk_reviewer\src\treemap\mod.rs (现有 stub)
  </read_first>
  <action>
    新建 src/treemap/types.rs，定义 TreemapNode 结构体（D-07 决策）。

    结构体字段（完全按 D-07 指定的字段）：
    ```rust
    use emath::{Pos2, Rect, Vec2};
    use egui::Color32;

    #[derive(Debug, Clone)]
    pub struct TreemapNode {
        pub rect: Rect,
        pub label: String,
        pub color: Color32,
        pub depth: u32,
        pub entry_index: usize,
        pub is_dir: bool,
        pub size: u64,
        pub percentage: f32,
    }
    ```

    同时在 src/treemap/mod.rs 中声明模块并 re-export：
    ```rust
    pub mod types;
    pub mod layout;
    pub mod color;

    pub use types::TreemapNode;
    pub use layout::layout_treemap;
    pub use color::FileCategory;
    ```

    注意：布局和颜色模块尚未实现，mod.rs 中的声明会导致编译错误，因此暂时只声明 types 模块。mod.rs 内容：
    ```rust
    pub mod types;

    pub use types::TreemapNode;
    ```

    不需要写测试——这是纯数据模型定义（TDD 适用性判断：跳过）。
  </action>
  <verify>
    <automated>cargo check 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check` 编译通过，无错误
    - `grep -c "pub struct TreemapNode" src/treemap/types.rs` 返回 1
    - `grep -c "pub rect: Rect" src/treemap/types.rs` 返回 1
    - `grep -c "pub use types::TreemapNode" src/treemap/mod.rs` 返回 1
    - `grep -c "pub mod types" src/treemap/mod.rs` 返回 1
    - TreemapNode 有且仅有 9 个字段：rect, label, color, depth, entry_index, is_dir, size, percentage
    - `#[derive(Debug, Clone)]` 在 TreemapNode 上
  </acceptance_criteria>
  <done>TreemapNode 结构体定义完整，treemap 模块可编译</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 app.rs 状态字段</name>
  <files>src/app.rs</files>
  <read_first>
    - D:\Develop\open_code\disk_reviewer\src\app.rs (当前完整结构体定义和 new()、start_scan()、consume_events()、update() 实现)
    - D:\Develop\open_code\disk_reviewer\src\scanner\types.rs (DirNode 结构)
    - D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-RESEARCH.md (Pattern 4: 下钻状态管理, 第 322-365 行)
    - D:\Develop\open_code\disk_reviewer\.planning\phases\02-treemap\02-PATTERNS.md (app.rs MODIFICATION 部分)
  </read_first>
  <action>
    修改 src/app.rs，为 DiskReviewerApp 添加 Phase 2 所需状态字段。

    **Struct 扩展** — 在现有字段后添加：
    ```rust
    pub struct DiskReviewerApp {
        pub drives: Vec<DriveInfo>,
        pub scan_result: Option<Arc<DirNode>>,
        pub scan_progress: Option<ScanEvent>,
        event_receiver: Option<Receiver<ScanEvent>>,
        pub status_message: String,
        cancel_token: Option<Arc<AtomicBool>>,
        // Phase 2: Treemap 状态
        pub nav_stack: Vec<usize>,
        pub selected_index: Option<usize>,
        pub treemap_nodes: Vec<crate::treemap::TreemapNode>,
    }
    ```

    **new() 扩展** — 添加新字段初始化：
    ```rust
    pub fn new(cc: &eframe::CreationContext<'_>) -> Self {
        let drives = drives::enumerate_drives();
        Self {
            drives,
            scan_result: None,
            scan_progress: None,
            event_receiver: None,
            status_message: "就绪".to_string(),
            cancel_token: None,
            nav_stack: Vec::new(),
            selected_index: None,
            treemap_nodes: Vec::new(),
        }
    }
    ```

    **consume_events() 扩展** — 在 ScanEvent::Complete 处理分支中，设置 current_node：
    ```rust
    ScanEvent::Complete { root, duration, total_files, access_denied_count } => {
        self.scan_result = Some(Arc::new(root.clone()));
        self.nav_stack = vec![0]; // 从根节点开始（根节点本身是 DirNode，nav_stack[0] 指向根的第一个子级视图）
        // 实际上根节点的 children 就是第一层视图
        self.rebuild_treemap();
        self.status_message = format!(
            "扫描完成: {} 个文件, 耗时 {:.1}s, {} 个目录无权限",
            total_files,
            duration.as_secs_f64(),
            access_denied_count
        );
    }
    ```

    等等——nav_stack 语义需要明确。根据 RESEARCH.md Pattern 4，nav_stack 存储的是 children 索引路径。根视图显示 scan_result 的 children，所以初始 nav_stack 应该是空 Vec（表示在根层级）。

    修正：
    ```rust
    self.nav_stack = Vec::new(); // 空 = 根层级
    ```

    **添加辅助方法** — 在 impl DiskReviewerApp 中添加：
    ```rust
    fn current_dir(&self) -> Option<&DirNode> {
        let root = self.scan_result.as_ref()?;
        let mut current = root.as_ref();
        for &idx in &self.nav_stack {
            current = current.children.get(idx).and_then(|e| match e {
                Entry::Dir(d) => Some(d),
                _ => None,
            })?;
        }
        Some(current)
    }

    fn rebuild_treemap(&mut self) {
        if let Some(dir) = self.current_dir() {
            // 暂时为空实现，plan 02-02 实现 layout_treemap 后替换
            // self.treemap_nodes = crate::treemap::layout_treemap(dir);
        }
    }
    ```

    注意：rebuild_treemap() 中暂时不调用 layout_treemap，因为该函数在 plan 02-02 才实现。此处留注释占位。

    **不需要修改 update() 方法** — UI 布局修改在 plan 02-03/02-05 中完成。
  </action>
  <verify>
    <automated>cargo check 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check` 编译通过，无错误
    - `grep -c "pub nav_stack: Vec<usize>" src/app.rs` 返回 1
    - `grep -c "pub selected_index: Option<usize>" src/app.rs` 返回 1
    - `grep -c "pub treemap_nodes: Vec" src/app.rs` 返回 1
    - `grep -c "fn current_dir" src/app.rs` 返回 1
    - `grep -c "fn rebuild_treemap" src/app.rs` 返回 1
    - `grep -c "nav_stack = Vec::new()" src/app.rs` 返回 1
  </acceptance_criteria>
  <done>app.rs 扩展了 treemap 状态字段和辅助方法，编译通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无 | 本 plan 仅涉及数据结构和状态字段，无外部输入或信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | — | — | accept | 纯数据结构定义，无安全威胁 |
</threat_model>

<verification>
cargo check 编译通过，无错误。TreemapNode 结构体定义完整，app.rs 状态字段已添加。
</verification>

<success_criteria>
- treemap/types.rs 定义了 TreemapNode 结构体，包含全部 9 个字段
- treemap/mod.rs 正确声明模块并 re-export
- app.rs 增加了 nav_stack, selected_index, treemap_nodes 三个字段
- app.rs 增加了 current_dir() 和 rebuild_treemap() 辅助方法
- 扫描完成事件触发时 nav_stack 初始化为空，rebuild_treemap() 被调用
- cargo check 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/02-treemap/02-01-SUMMARY.md`
</output>
