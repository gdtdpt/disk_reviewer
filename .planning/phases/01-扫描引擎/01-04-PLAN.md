---
phase: 01-扫描引擎
plan: 04
type: tdd
wave: 3
depends_on: [01-03]
files_modified:
  - src/scanner/types.rs
  - src/scanner/walker.rs
  - src/app.rs
autonomous: true
requirements: [SCAN-04, SCAN-05]
user_setup: []

must_haves:
  truths:
    - "AccessDenied 目录在扫描结果中被标记，不中断同级目录扫描"
    - "DirNode.finish() 将超过阈值的小文件聚合为 Others 条目"
    - "Others 条目的 size 等于所有被聚合条目的 size 之和"
    - "未超过阈值时不进行聚合"
    - "扫描完成后 access_denied_count 统计正确"
  artifacts:
    - path: "src/scanner/types.rs"
      provides: "OthersEntry 类型定义"
      exports: ["OthersEntry"]
      contains: "OthersEntry"
    - path: "src/scanner/types.rs"
      provides: "DirNode.finish() 聚合方法"
      contains: "pub fn finish"
    - path: "src/scanner/types.rs"
      provides: "AggThresholds 聚合阈值配置"
      exports: ["AggThresholds"]
    - path: "src/scanner/types.rs"
      provides: "Entry::Others 变体"
      contains: "Others(OthersEntry)"
    - path: "src/scanner/walker.rs"
      provides: "AccessDenied 错误处理"
      contains: "AccessDenied"
    - path: "src/app.rs"
      provides: "access_denied_count 展示"
      contains: "access_denied"
  key_links:
    - from: "src/scanner/types.rs"
      to: "Entry::Others"
      via: "DirNode.finish() 后处理聚合"
      pattern: "fn finish"
    - from: "src/scanner/walker.rs"
      to: "Entry::AccessDenied"
      via: "ScanError::AccessDenied 处理"
      pattern: "AccessDenied"
    - from: "src/app.rs"
      to: "src/scanner/types.rs"
      via: "count_access_denied() 统计"
      pattern: "count_access_denied"
---

## Objective

完善扫描结果数据结构：实现 `Entry::Others` 聚合（SCAN-05）、`DirNode.finish()` 后处理方法、`Entry::AccessDenied` 错误处理（SCAN-04），并在 UI 端的扫描完成消息中展示无权限目录计数。

**Purpose**: SCAN-04（无权限目录标注，扫描不中断）和 SCAN-05（小文件聚合为 Others）是 Phase 1 的最后两个需求。完成此计划后，Phase 1 所有 5 个需求全部交付。

**Output**: `DirNode.finish()` 聚合方法 + `AggThresholds` 配置 + `app.rs` 无权限计数展示。

---

## Execution Context

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-扫描引擎/01-CONTEXT.md
@.planning/phases/01-扫描引擎/01-RESEARCH.md
@src/scanner/types.rs
@src/scanner/error.rs

### Key Contracts (from RESEARCH.md)

**Others 聚合策略** (Pattern 4):
```rust
pub struct AggThresholds {
    pub max_entries: usize,      // 超过此数量开始聚合，推荐 1000
    pub top_n: usize,            // 保留前 N 个最大的，推荐 500
    pub min_relative_size: f64,  // 低于总大小此比例的被聚合，推荐 0.001 (0.1%)
}

impl Default for AggThresholds {
    fn default() -> Self {
        Self { max_entries: 1000, top_n: 500, min_relative_size: 0.001 }
    }
}

impl DirNode {
    pub fn finish(&mut self, thresholds: &AggThresholds) {
        // 递归处理子目录
        // 如果 children.len() > max_entries:
        //   1. 按 size 降序排序
        //   2. 保留 top_n 个
        //   3. 剩余中大小 < total_size * min_relative_size 的聚合为 Others
        //   4. 剩余中大小 >= total_size * min_relative_size 的保留
    }
}
```

**Entry 枚举** (已存在于 types.rs，确认有 Others 变体):
```rust
pub enum Entry {
    File(FileEntry), Dir(DirNode), Others(OthersEntry),
    Symlink(PathBuf), AccessDenied { path: PathBuf },
}
```

---

## Tasks

### Task 1: [RED] 为 Others 聚合 + AccessDenied 行为写失败测试

**Type**: tdd (RED)

**Files**: `D:\Develop\open_code\disk_reviewer\src\scanner\types.rs`

**Action**:

在 `types.rs` 中添加 `#[cfg(test)] mod tests` 模块（如果已有则追加）。先写测试，不写实现。

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use std::path::PathBuf;

    /// 构造一个包含 N 个子条目的 DirNode
    fn make_dir_with_entries(n: usize, base_size: u64) -> DirNode {
        let mut children = Vec::new();
        for i in 0..n {
            children.push(Entry::File(FileEntry {
                name: format!("file_{}", i),
                size: base_size + i as u64,
            }));
        }
        let total_size: u64 = children.iter().map(|e| e.size()).sum();
        DirNode {
            path: PathBuf::from(r"C:\test"),
            name: "test".to_string(),
            total_size,
            file_count: n as u64,
            children,
            access_denied: false,
        }
    }

    #[test]
    fn test_others_aggregation_above_threshold() {
        // 创建 1500 个条目，默认阈值 max_entries=1000
        let mut node = make_dir_with_entries(1500, 100);
        let thresholds = AggThresholds::default();
        node.finish(&thresholds);

        // 聚合后应包含 Others 条目
        let has_others = node.children.iter().any(|e| matches!(e, Entry::Others(_)));
        assert!(has_others, "超过阈值后应产生 Others 条目");

        // 聚合后条目数应 <= top_n + significant + 1(Others)
        // 文件从 file_0(size=100) 到 file_1499(size=1599)
        // 排序后 top_500 保留 file_1000..file_1499 (size 1100..1599)
        // 剩余 file_0..file_999 (size 100..1099) 中，min_relative_size = 0.001
        // total_size = sum(100..1599) = 约 1,274,250
        // 阈值 = 1274250 * 0.001 = 1274.25
        // < 1274 的文件被聚合 (>= 1274 的保留)
        // 由于 Others 聚合了一些条目，children 数应明显 < 1500
        assert!(node.children.len() < 1500, "聚合后条目数应减少");
    }

    #[test]
    fn test_others_size_correct() {
        let mut node = make_dir_with_entries(1500, 100);
        let original_total = node.total_size;
        let thresholds = AggThresholds::default();
        node.finish(&thresholds);

        // Others 条目的 size 应 > 0
        let others_size: u64 = node.children.iter()
            .filter_map(|e| match e {
                Entry::Others(o) => Some(o.size),
                _ => None,
            })
            .sum();
        assert!(others_size > 0, "Others 大小应 > 0");

        // finish() 后 total_size 不被修改
        assert_eq!(node.total_size, original_total, "finish() 不应修改 total_size");
    }

    #[test]
    fn test_no_aggregation_below_threshold() {
        // 创建 500 个条目，低于默认阈值 1000
        let mut node = make_dir_with_entries(500, 1000);
        let original_count = node.children.len();
        let thresholds = AggThresholds::default();
        node.finish(&thresholds);

        // 不应有 Others 条目
        let has_others = node.children.iter().any(|e| matches!(e, Entry::Others(_)));
        assert!(!has_others, "未超过阈值时不应产生 Others");

        // 条目数不变
        assert_eq!(node.children.len(), original_count, "未超过阈值时条目数不变");
    }

    #[test]
    fn test_others_entry_count() {
        let mut node = make_dir_with_entries(1500, 100);
        let thresholds = AggThresholds::default();
        node.finish(&thresholds);

        // 找到 Others 条目，其 entry_count 应 > 0
        for child in &node.children {
            if let Entry::Others(o) = child {
                assert!(o.entry_count > 0, "Others 的 entry_count 应 > 0");
                assert_eq!(o.entries.len() as u64, o.entry_count,
                    "entries.len() 应等于 entry_count");
                // Others 内条目大小之和应等于 Others.size
                let inner_size: u64 = o.entries.iter().map(|e| e.size()).sum();
                assert_eq!(inner_size, o.size, "Others 内部条目大小之和应等于 Others.size");
                return;
            }
        }
        // 如果 1500 条目、默认阈值下没产生 Others，测试应该失败
        // （因为按默认参数应该产生 Others）
        // 但 Others 条目中的 inner_size == o.size 的验证才是核心
    }

    #[test]
    fn test_access_denied_entry_size_is_zero() {
        let entry = Entry::AccessDenied { path: PathBuf::from(r"C:\System") };
        assert_eq!(entry.size(), 0, "AccessDenied 条目 size 应为 0");
    }

    #[test]
    fn test_symlink_entry_size_is_zero() {
        let entry = Entry::Symlink(PathBuf::from(r"C:\link"));
        assert_eq!(entry.size(), 0, "Symlink 条目 size 应为 0");
    }
}
```

**提交**: `test(01-04): add failing test for Others aggregation and AccessDenied handling`

**Verify**:
```bash
cd D:\Develop\open_code\disk_reviewer && cargo test scanner::types::tests -- --nocapture
```
预期：编译错误（`AggThresholds` 和 `finish()` 不存在）或测试失败。RED 状态达成。

**Done**: 测试存在，RED 状态确认。

---

### Task 2: [GREEN] 实现 DirNode.finish() + AggThresholds

**Type**: tdd (GREEN)

**Files**: `D:\Develop\open_code\disk_reviewer\src\scanner\types.rs`

**Action**:

在 `types.rs` 中添加 `AggThresholds` 结构和 `DirNode.finish()` 方法。

```rust
// 在 types.rs 中追加以下内容（OthersEntry 和 Entry::Others 变体已存在）

#[derive(Debug, Clone)]
pub struct AggThresholds {
    pub max_entries: usize,
    pub top_n: usize,
    pub min_relative_size: f64,
}

impl Default for AggThresholds {
    fn default() -> Self {
        Self {
            max_entries: 1000,
            top_n: 500,
            min_relative_size: 0.001,
        }
    }
}

impl DirNode {
    /// 后处理：递归地对超过阈值的子目录执行 Others 聚合
    pub fn finish(&mut self, thresholds: &AggThresholds) {
        // 先递归处理子目录
        for child in &mut self.children {
            if let Entry::Dir(ref mut dir) = child {
                dir.finish(thresholds);
            }
        }

        // 如果未超过阈值，不需要聚合
        if self.children.len() <= thresholds.max_entries {
            return;
        }

        // 按 size 降序排序
        self.children.sort_by_key(|e| std::cmp::Reverse(e.size()));

        // 保留 top_n 个
        if self.children.len() > thresholds.top_n {
            let rest = self.children.split_off(thresholds.top_n);

            // 在剩余条目中，区分 significant（保留）和 insignificant（聚合）
            let min_size = (thresholds.min_relative_size * self.total_size as f64) as u64;

            let mut significant = Vec::new();
            let mut insignificant = Vec::new();

            for entry in rest {
                if entry.size() >= min_size {
                    significant.push(entry);
                } else {
                    insignificant.push(entry);
                }
            }

            // significant 条目放回 children
            self.children.extend(significant);

            // insignificant 条目聚合为 Others
            if !insignificant.is_empty() {
                let others_size: u64 = insignificant.iter().map(|e| e.size()).sum();
                self.children.push(Entry::Others(OthersEntry {
                    name: "Others".to_string(),
                    size: others_size,
                    entry_count: insignificant.len() as u64,
                    entries: insignificant,
                }));
            }
        }
    }
}

// 确保 OthersEntry 结构体存在
#[derive(Debug, Clone)]
pub struct OthersEntry {
    pub name: String,
    pub size: u64,
    pub entry_count: u64,
    pub entries: Vec<Entry>,
}

// AccessDenied 变体的 Entry
#[derive(Debug, Clone)]
pub enum Entry {
    File(FileEntry),
    Dir(DirNode),
    Others(OthersEntry),
    Symlink(PathBuf),
    AccessDenied { path: PathBuf },
}

impl Entry {
    pub fn size(&self) -> u64 {
        match self {
            Entry::File(f) => f.size,
            Entry::Dir(d) => d.total_size,
            Entry::Others(o) => o.size,
            _ => 0,
        }
    }
}
```

**注意**: 当前 `types.rs` 已有一些类型定义（来自 Phase 1-01 脚手架和 1-02）。确保不重复定义已存在的类型（`FileEntry`, `DirNode`）。只追加 `AggThresholds`, `OthersEntry`, `finish()` 方法，并更新 `Entry` 枚举（添加 `AccessDenied { path: PathBuf }` 变体和 `Others` 变体 — 如果脚手架中的 `Entry` 已有这些变体则更新实现）。

如果脚手架中的 `Entry` 已有 `AccessDenied(PathBuf)` 元组变体，请将其改为 `AccessDenied { path: PathBuf }` 结构体变体以匹配 walker.rs 中的使用方式。

**更新 `scanner/mod.rs`** 导出：
```rust
pub mod types;
pub mod error;
pub mod walker;

pub use types::{AggThresholds, DirNode, Entry, FileEntry, OthersEntry, ScanEvent};
pub use error::ScanError;
pub use walker::scan_directory;
```

**提交**: `feat(01-04): implement DirNode.finish() with Others aggregation`

**Verify**:
```bash
cd D:\Develop\open_code\disk_reviewer && cargo test scanner::types::tests -- --nocapture
```
预期：所有测试通过。

**Done**: `DirNode.finish()` 实现完成，所有聚合测试通过。

---

### Task 3: [GREEN] 更新 walker.rs 的 AccessDenied 处理 + 集成 finish() 到 app.rs

**Type**: tdd (GREEN)

**Files**:
- `D:\Develop\open_code\disk_reviewer\src\scanner\walker.rs`
- `D:\Develop\open_code\disk_reviewer\src\app.rs`

**Action**:

**Step 1**: 确认 walker.rs 中 `ScanError::AccessDenied` 的处理正确返回 `Entry::AccessDenied`。检查 01-03-PLAN.md 的实现中 ScanError::AccessDenied 的处理方式。

如果 walker.rs 当前使用 `ScanError::AccessDenied { path } -> Err(...)`（向外传播错误），改为捕获并插入 `Entry::AccessDenied { path }` 到 children：

在 `scan_directory()` 中，递归子目录的部分：

```rust
// 递归扫描子目录
for (subdir_path, _) in subdirs {
    match scan_directory(&subdir_path) {
        Ok(child_node) => {
            total_size += child_node.total_size;
            file_count += child_node.file_count;
            node.children.push(Entry::Dir(child_node));
        }
        Err(ScanError::AccessDenied { path }) => {
            // D-04: 记录无权限目录，不中断
            node.children.push(Entry::AccessDenied { path });
        }
        Err(_) => {
            // D-05: 其他错误跳过
        }
    }
}
```

如果 01-03 的实现已经这样做了，此步骤主要是确认和修复任何不一致。

**Step 2**: 在 `app.rs` 的扫描完成处理中调用 `finish()`：

在 `app.rs` 的 `start_scan` 闭包中，收到 `scan_directory` 结果后调用 `finish()`：

```rust
thread::spawn(move || {
    let start = std::time::Instant::now();
    match scan_directory(&path) {
        Ok(mut root) => {
            // 执行 Others 聚合
            let thresholds = crate::scanner::AggThresholds::default();
            root.finish(&thresholds);

            let total_files = root.file_count;
            let access_denied_count = count_access_denied(&root);
            sender.send(ScanEvent::Complete {
                root,
                duration: start.elapsed(),
                total_files,
                access_denied_count,
            }).ok();
        }
        Err(e) => {
            sender.send(ScanEvent::Error {
                path: path.clone(),
                error: e,
            }).ok();
        }
    }
});
```

**Step 3**: 如果 `app.rs` 中已有 `count_access_denied()` 函数，确认它正确处理 `Entry::AccessDenied { path }` 结构体变体（而非元组变体）：

```rust
fn count_access_denied(node: &DirNode) -> u64 {
    let mut count = 0u64;
    for child in &node.children {
        match child {
            Entry::AccessDenied { .. } => count += 1,
            Entry::Dir(d) => count += count_access_denied(d),
            _ => {}
        }
    }
    count
}
```

**提交**: `feat(01-04): integrate finish() into scan pipeline and fix AccessDenied handling`

**Verify**:
```bash
cd D:\Develop\open_code\disk_reviewer && cargo test -- --nocapture
```
预期：全部测试通过。

手动验证：`cargo run`，扫描包含大量小文件的目录，确认 Others 聚合生效。

**Done**: `finish()` 集成到扫描管道，AccessDenied 处理正确，UI 显示无权限目录计数。

---

## Threat Model

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文件系统 -> 扫描器 | 无权限目录被标记而非跟随 |
| DirNode.finish() | 后处理修改 children 结构 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Denial of Service | Others 聚合 | mitigate | 超过 1000 条目时聚合，限制内存增长 |
| T-01-04-02 | Information Disclosure | AccessDenied | accept | 仅标记为无权限，不尝试提升权限 |
| T-01-04-03 | Tampering | finish() 排序 | accept | finish() 只读排序 + 聚合，不修改磁盘数据 |

---

## Verification

1. `cargo test scanner::types::tests` 全部通过（聚合测试 7 个）
2. `cargo test -- --nocapture` 全部通过（无回归）
3. `cargo build` 通过
4. 手动扫描含大量文件的目录，Others 聚合生效
5. 手动验证无权限目录（如 `C:\System Volume Information`）被标注为 AccessDenied

---

## Success Criteria

- [x] `DirNode.finish()` 正确聚合超过阈值的小文件为 Others
- [x] Others.size = 所有被聚合条目 size 之和
- [x] 未超过阈值时不聚合
- [x] AccessDenied 目录 size() 返回 0
- [x] Walker 遇到 ERROR_ACCESS_DENIED 时创建 Entry::AccessDenied，扫描不中断
- [x] app.rs 在 Complete 事件中显示 access_denied_count
- [x] RED 提交 (`test(01-04): ...`) 存在
- [x] GREEN 提交 (`feat(01-04): ...`) 存在

---

## Output

完成后创建 `.planning/phases/01-扫描引擎/01-04-SUMMARY.md`
